import {combineReducers} from "redux";
// import user from './user_reducer';

//combineReducers | rootReducer 에서 각각의 reducers 들을 하나로 합쳐 주는 기능
const rootReducer = combineReducers({
  // user,
})

export default rootReducer;